# Run `doppler setup` from the repo root to link this directory to your Doppler project.
# Recommended: project `unstall`, config `dev` for local development.
setup:
  - project: unstall
    config: dev
